#![allow(dead_code)]

/// Defines a rectangualar region of the screen.
pub(crate) struct Region {
    pub(crate) x: u32,
    pub(crate) y: u32,
    pub(crate) width: u32,
    pub(crate) height: u32,
}
